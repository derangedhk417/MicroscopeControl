{
	"description" : "Given a directory that has already been processed by ScanProcessor.py, this program will load all of the images and corresponding metadata from them and will produce a summary file containing the location of all of the flakes that it has found and their properties.",
	"arguments"   : [
		{
			"names" : ["-d", "--image-directory"],
			"spec"  : {
				"dest"     : "image_directory",
				"type"     : "str",
				"required" : true,
				"help"     : "The directory containing the images to process."
			}
		},
		{
			"names" : ["-np", "--n-processes"],
			"spec"  : {
				"dest"     : "n_processes",
				"type"     : "int",
				"required" : true,
				"help"     : "The number of processes to use when processing images."
			}
		},
		{
			"names" : ["-ds", "--downscale"],
			"spec"  : {
				"dest"     : "downscale",
				"type"     : "float",
				"default"  : 0.2,
				"help"     : "How much to downscale images along each dimension before processing."
			}
		}
	]
}