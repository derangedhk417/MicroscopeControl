{
	"description" : "Monitors the specified directory for images and attempts to extract flake geometry from them. If configured correctly, this program will also delete images that don't contain any flakes in order to save disk space.",
	"arguments"   : [
		{
			"names" : ["-d", "--image-directory"],
			"spec"  : {
				"dest"     : "output_directory",
				"type"     : "str",
				"required" : true,
				"help"     : "The directory containing the images to process."
			}
		},
		{
			"names" : ["-m", "--material-file"],
			"spec"  : {
				"dest"     : "material_file",
				"type"     : "str",
				"required" : true,
				"help"     : "A file containing information about the expected optical contrast of the material being imaged. See calibration_data/_graphene_on_90nmSiO2_Silicon.json for an example of what this file should look like. These files can be generated with the MonolayerContrastCalculator program (in a separate repository)."
			}
		},
		{
			"names" : ["-np", "--n-processes"],
			"spec"  : {
				"dest"     : "n_processes",
				"type"     : "int",
				"required" : true,
				"help"     : "The number of concurrent processes to use when processing files. This program uses a process pool to delegate work to multiple cores and/or processors."
			}
		},
		{
			"names" : ["-nl", "--n-layers-max"],
			"spec"  : {
				"dest"     : "n_layers_max",
				"type"     : "int",
				"default"  : 10,
				"help"     : "The maximum number of layers to include in the final database generated by this code."
			}
		},
		{
			"names" : ["-id", "--image-dimensions"],
			"spec"  : {
				"dest"     : "image_dims",
				"nargs"    : 2,
				"type"     : "float",
				"required" : true,
				"help"     : "The dimensions of images at zoom they were taken at, in millimenters. Format is (height, width)."
			}
		}
	]
}